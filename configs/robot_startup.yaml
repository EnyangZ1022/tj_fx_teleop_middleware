robot_startup:
  vel_ratio: 20
  acc_ratio: 20

  ready_joint_deg:
    left: [90, -60, -90, -90, 0, 0, 0]
    right: [90, 60, -90, -90, 0, 0, 0]

  home:
    send_hz: 100.0
    timeout_s: 20.0
    tol_deg: 1.0
    stable_samples: 20
    check_period_s: 0.01
    pre_wait_s: 2.0

  # Do not overwrite IK reference config.
