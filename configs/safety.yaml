safety:
  pico_timeout_ms: 300.0
  enable:
    field: grip
    on_threshold: 0.85
    off_threshold: 0.65
    mode: hold
  target:
    max_single_step_mm: 50.0
    max_velocity_mm_s: 500.0
  calibration:
    require_both_arms_calibrated: false
  operation:
    allow_single_arm_motion: true
